# Required: credential for the configured OpenAI-compatible endpoint.
API_KEY=

# Defaults for the Self-Collaboration model backend.
MODEL=moonshotai/kimi-k2.5
BASE_URL=https://openrouter.ai/api/v1

# Required when launching Harbor on Windows so benchmark instructions use UTF-8.
PYTHONUTF8=1
